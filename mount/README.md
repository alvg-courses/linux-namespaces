## Part-3  Mount Namespace

Create minimal linux installation i.e. rootfs.
```
$> ./mount sh
%> ps
%> 
```
The ```ps``` command will list only processes in that namespace.

Compare these commands to a different shell, running on default namespaces.

